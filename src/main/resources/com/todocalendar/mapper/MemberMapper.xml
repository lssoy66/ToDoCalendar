<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.todocalendar.mapper.MemberMapper">

	<insert id="insertMember">
		insert into member(member_no, id, password, name, email, join_date, membership) values (member_no_seq.nextval, #{id}, #{password}, #{name}, #{email}, SYSDATE, #{membership})
	</insert>
	
	<select id="readById" resultType="com.todocalendar.model.MemberVO">
		select * from member where id = #{id}
	</select>
	
	<select id="loginCheck" resultType="com.todocalendar.model.MemberVO">
		select * from member where id = #{id} and password = #{password}
	</select>

</mapper>